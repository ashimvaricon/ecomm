export enum ROUTES {
    Home = "/",
    Products = "/products",
    ProductDetails = "/products/:slug",
    Cart = "/cart",
    Checkout = "/checkout",
    Orders = "/orders",
    Profile = "/profile",
    NotFound = "*",
}
