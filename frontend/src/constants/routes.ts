export enum ROUTES {
    Home = "/",
    Products = "/products",
    ProductDetails = "/products/:id",
    Cart = "/cart",
    Checkout = "/checkout",
    Orders = "/orders",
    Profile = "/profile",
    NotFound = "*",
}
